{
  "name": "tspark",
  "private": true,
  "workspaces": [
    "packages/client",
    "packages/api",
    "packages/common"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "test": "npm run test --workspaces --if-present",
    "start:dev": "concurrently -n \"common-watch,api-watch,client,api\" \"npm run watch -w=@tspark/common -- --preserveWatchOutput\" \"npm run watch -w=@tspark/api -- --preserveWatchOutput\" \"npm run start:dev -w=@tspark/client\" \"npm run start:dev -w=@tspark/api\""
  }
}
